# Test coverage output (npm run test:coverage — c8 lcov report). Generated by
# the CI/test container, bind-mounted out to the host for artifact upload —
# never needed inside the production image build context.
coverage/
.v8-coverage/

node_modules/
