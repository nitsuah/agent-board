{
  "name": "agent-dashboard",
  "version": "0.1.0",
  "description": "Lightweight local agent dashboard with model selector",
  "type": "module",
  "scripts": {
    "dev": "node server.js",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --experimental-vm-modules tests/review-blockers.js && node --experimental-vm-modules tests/demo-mode.js && node --experimental-vm-modules tests/metrics-api.js && node --experimental-vm-modules tests/functional-health.js && node --experimental-vm-modules tests/e2e-chat.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "axios": "^1.6.0"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "@vitejs/plugin-react": "^4.2.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
}
